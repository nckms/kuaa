[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build", "npx prisma generate", "npx prisma migrate deploy"]

[start]
cmd = "node dist/index.js"
